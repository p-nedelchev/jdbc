CREATE TABLE contacts (
contact_id          SERIAL,
first_name          varchar(20),
last_name           varchar(20),
email               varchar(50),
phone               varchar(15),
PRIMARY KEY(contact_id)
)